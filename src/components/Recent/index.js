import React from "react";
import NewBooks from "../HomePage/NewBooks";

const Recent = () => {
  return (
    <div style={{padding: '100px 0 0 0'}} id="recent">
      <NewBooks />
    </div>
  );
};

export default Recent;
